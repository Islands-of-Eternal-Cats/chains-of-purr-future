## 1. Release Version

- [x] 1.1 Bump package metadata to 0.2.0 and inject the package version as a typed Vite build constant.
- [x] 1.2 Render the prefixed application version in the brand header and cover it with an application test.

## 2. Unassigned Crew Roster

- [x] 2.1 Derive all cats without persistent work assignments and reuse their IDs for rest-seat highlighting.
- [x] 2.2 Render the roster count, compact cat state buttons, empty state, and existing selection interaction in the crew panel.
- [x] 2.3 Add tests for roster inclusion and exclusion across states, selection toggling, and removal after assignment.

## 3. Control Panel Layout

- [x] 3.1 Move network guidance into normal sidebar flow and adjust desktop and mobile styling so controls remain reachable without overlap.

## 4. Verification

- [x] 4.1 Run the full unit test suite, production build, and strict OpenSpec validation.
- [x] 4.2 Visually verify the sidebar at 1280x720 and at the mobile breakpoint, including scrolling, selection, and non-overlapping guidance.
